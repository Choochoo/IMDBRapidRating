# Optional server-only development settings for IMDb Rapid Rater.

# Optional: set true to test the UI/server path without sending anything to IMDb.
IMDB_DRY_RUN=false

# IMDb connection data, API keys, imported ratings, and progress belong in the
# website UI. They are stored in each visitor's browser, not in this file.
